#include <tuple>
#include <vector>
#include <iostream>
#include <utility>
#include <queue>

#define max 1001

int capM[max][max];

int neighborM[max][max];

//int source;

//int sink;

vector<tuple<int,int,int>> courierLoads(const vector<int> &orders, const vector<tuple<int,int,int>> &capacities) {

  vector<tuple<int,int,int>> flows;

  return flows;
}

tuple<int,int> edmondsKarp(int caps[a][b], int neighbors[c][d], int source, int sink) {
  int iFlow = 0;
  vector<vector<int>> residualCaps(max, vector<int> max);

  while(true) {
    
  }
}

tuple<int,int> breadthFirstSearch(int caps[a][b], int neighbors[c][d], int source, int sink, int initialFlow) {
  
  vector<int> parents(max);
  for(
